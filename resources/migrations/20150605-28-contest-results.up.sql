CREATE TABLE contest_results (id BIGINT NOT NULL,
                              results_id BIGINT REFERENCES results (id) NOT NULL,
                              PRIMARY KEY (results_id, id),
                              contest_id BIGINT,
                              jurisdiction_id BIGINT,
                              entire_district BOOLEAN NOT NULL,
                              total_votes BIGINT,
                              total_valid_votes BIGINT,
                              overvotes BIGINT,
                              blank_votes BIGINT,
                              accepted_provisional_votes BIGINT,
                              rejected_votes BIGINT,
                              certification TEXT);
