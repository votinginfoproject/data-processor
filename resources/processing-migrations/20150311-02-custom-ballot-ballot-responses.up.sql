CREATE TABLE custom_ballot_ballot_responses (custom_ballot_id INTEGER,
                                             ballot_response_id INTEGER,
                                             sort_order INTEGER);
