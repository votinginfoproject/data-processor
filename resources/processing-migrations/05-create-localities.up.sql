CREATE TABLE localities (id INTEGER PRIMARY KEY,
                         name TEXT,
                         state_id INTEGER,
                         type TEXT,
                         election_administration_id INTEGER);
